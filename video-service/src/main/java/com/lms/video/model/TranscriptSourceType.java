package com.lms.video.model;

public enum TranscriptSourceType {
    FEATURED,       // featured-content session videos (existing flow)
    LIBRARY_VIDEO,  // regular course/module videos (new flow)
    COURSE_VIDEO
}